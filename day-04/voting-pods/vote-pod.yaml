apiVersion: v1
kind: Pod
metadata:
  name: vote
  labels:
    app: vote
    name: vote
spec:
  containers:
  - name: vote
    image: dockersamples/examplevotingapp_vote
    ports:
    - containerPort: 80
