ยกสถานการณ์ที่ Kubernates เข้ามาแก้ไขปัญหา
[
1. auto restart container
2. auto scale service ตาม triffic ด้วยคำสั่ง kubectl scale
3. rolling update -> pod ใหม่ขึ้น pod เก่ายังใช้งานได้ -> zero downtime
4. สร้างช่องทางการสื่อสารระหว่าง container
5. กระจาย container ตาม server ที่มี cpu/memory อัติโนมัติ
]
Use Case ยอดนิยมที่องค์กรเลือกมาใช้ Kubernates
[
รันแอปจำนวนมากแบบอัติโนมัติบนหลายเครื่อง/หลาย Cloud
1. ต้องการ scale
2. ต้องการ downtime น้อย
3. triffic ไม่คงที่ -> ต้องการ auto scale
4. ใช้ multi cloud หรือ hybrid on-premise + cloud
5. batch workload -> ปิดเมื่อจบงาน
]
Use Case ที่ไม่เหมาะที่จะนำ Kubernates มาใช้
[
1. แอปเล็ก / traffic น้อย
2. legacy app ที่ผูกติดกับ os
]
ประวัติของ Kubernates
[Kubernetes เกิดจากประสบการณ์รัน container ระดับใหญ่ของ Google แล้วถูกเปิดเป็นโอเพนซอร์ส และส่งต่อให้ CNCF จนกลายเป็นมาตรฐานจัดการ container ของอุตสาหกรรม]
สามารถใช้ Kubernates บน Local Windows ได้หรือไม่
[ได้ เช่น ใช้ minikube]
minikute คืออะไร ทำหน้าที่อะไร แนะนำ Use Case ยอดนิยมหน่อย
[
minikube = Kubernetes จำลองบนเครื่องคุณ สำหรับเรียนและทดลอง — ทำหน้าที่สร้าง cluster ท้องถิ่นให้ใช้ kubectl ได้เหมือนของจริง
มี Control Plane และ Worker ในเครื่องเดียว เหมือน K8s จริง
ใช้ kubectl ได้เหมือน Production
ช่วย Expose บริการออกนอก
ไม่ใช่ Production Cluster
ไม่เหมาะกับ Load จริง
ไม่แทน GKE / EKS / AKS ในองค์กร
]
kubectl คืออะไร ทำหน้าที่อะไร แนะนำ Use Case ยอดนิยมหน่อย
[
kubectl คือรีโมทคอนโทรลของ Kubernetes — ใช้สั่งดู สร้าง แก้ ลบ และ debug ทุกอย่างใน cluster ผ่าน API Server
หน้าที่ ดูสถานะ
จัดการทรัพยากร
เข้าไปใน Container
จัดการ Scale Rollout
Deploy แอป
ตรวจสุขภาพ Cluster
Debug ตอน Pod ไม่ขึ้น
]
ทำไมถึงต้องใช้ minikube คู่กับ kubectl
[minikube ทำให้มี Kubernetes ในเครื่อง — kubectl ทำให้คุณควบคุมมันได้
minikube สร้างและรัน Clusteer
kubectl สั่งงาน Cluster
ใน Cloud จริงก็ใช้ kubectl
]
API Server คืออะไร ทำหน้าที่อะไร
[API Server (kube-apiserver) อยู่ใน Control Plane (Master) ทำหน้าที่รับเรื่องของ Kubernetes — รับคำสั่ง ตรวจสิทธิ์ ตรวจความถูกต้อง แล้วบันทึก/ดึงสถานะจาก etcd ให้ทั้ง cluster
ติดต่อกับ Controller / Scheduler / kubelet
]
etcd คืออะไร ทำหน้าที่อะไร
[etcd คือสมุดบัญชีของ Kubernetes — เก็บว่า cluster “ควรเป็นแบบไหน” และ “ตอนนี้เป็นแบบไหน” ไว้ทั้งหมด
อยู่ใน Control Plane (Master)
เป็นฐานข้อมูล Key-value เก็บสถานะ (config/state) ทั้งหมดของ Cluster
ติดต่อผ่าน API Server -> etcd
รองรับ HA รันหลายตัวปล้องกันข้อมูลหาย
]
Control Plane (Master) คืออะไร ทำหน้าที่อะไร
[Control Plane คือศูนย์บัญชาการของ Kubernetes — รับคำสั่ง เก็บสถานะ จัดวาง Pod และคอยบังคับให้ cluster เป็นไปตามที่คุณประกาศไว้]
Worker Node คืออะไร ทำหน้าที่อะไร
Pod คืออะไร ทำหน้าที่อะไร
ไฟล์ lab1-commands.ps ทำหน้าที่อะไร ทำงานอย่างไร
kubectl expose deployment hello-minikube --type=NodePort --port=8080 ทำไมใช้ port 8080 แต่ port ที่ใช้เข้าถึงผ่าน localhost จึงเป็นเลขสุ่ม
[--port=8080 = พอร์ตของ Service ใน Kubernetes
พอร์ตบน localhost = ทางลัดที่ minikube เปิดให้ Windows เข้าถึง — คนละชั้น จึงเป็นเลขสุ่มได้]
NodePort คืออะไร ทำงานอย่างไร มีหน้าที่อะไรบ้าง
[Service ที่เปิดช่องทางเข้าถึงจากภายนอก
รวมหลาย Pod ให้เป็นปลายทางเดียว
ทำ Load Balancing
ให้ IP คงที่ แม้ Pod จะถูกสร้างหรือลบ	
]
1 Pod = 1 Container หมายถึง ถ้าเป็น Project เดียวกัน แต่แยก Frontend, Backend, Database จะเท่ากับ 3 Pod หรือไม่
[ใช่ แนะนำให้ใช้ 3 Pod แล้วสื่อสารกันด้วย ClusterIP]
ClusterIP คืออะไร ทำงานอย่างไร แนะนำ Use Case ที่เหมาะสม
[Service ที่เปิดการสื่อสารระหว่าง Pod แต่ไม่เปิดให้เข้าจากภายนอก Cluster
จัดการ DNS ให้แอปอื่นใน Cluster เรียกได้ด้วยชื่อ
kube-proxy กระจาย Traffic ไปยัง Pod ที่พร้อมใช้งาน
ถ้าต้องการให้เข้าถึงจากภายนอก ใช้ NodePort หรือ LoadBalancer แทน]
lab2-commands.ps1 ทำงานอย่างไร
[
1. ตรวจเครื่องมือ + เปิด Cluster
2. รันแบบ Imperative (kubectl run nginx) แล้วลบ
3. รันแบบ Declarative (apply nginx-pod.yaml)
4. Debug พบว่า ImagePullBackOff (Pull Image ไม่ได้) 
5. แก้ไขจุดผิด และ apply อีกครั้ง
6. สรุป
]
ไฟล์ lab3-commands.ps ทำหน้าที่อะไร ทำงานอย่างไร
[
1.ตรวจเครื่องมือ + เปิด Cluster
2. สร้าง ReplicaSet 3 Pod รอจน Running
3. ลบ Pod 1 ตัว แล้วรอจนกว่ากลับมาครบ 3 Pod
4. scale เป็น 5 แล้วลดเหลือ 2
5. 
]
อธิบายไฟล์ nginx-rs.yaml โดยละเอียด
[
สร้าง ReplicaSet ชื่อ nginx-rs
ต้องมี 3 Pod ที่ label app=nginx (replicas: 3)
แต่ละ Pod รัน nginx:1.25 พอร์ต 80
ถ้า Pod หาย → สร้างใหม่จาก template อัตโนมัติ (spec.template.spec.containers)
]
การ scale มันจะเลือก work node ใหม่หรือไม่
[ใช่ ถ้ามี node เดียวก็เลือก node เดิมนั้น]
ไฟล์ lab4-commands.ps ทำหน้าที่อะไร ทำงานอย่างไร
[
1. ตรวจสอบเครื่องมือ
2. Deployment 3 replicas รอจน rollout สำเร็จ
3. เช็คความพร้อม
4. เปลี่ยน Image แล้วรอ Rolling Update จบ
5. ดู Rollout History แล้ว Rollout Undo กลับเวอร์ชันก่อน
]
หลักการทำงานของ Rolling Update เป็นอย่างไร
[สร้าง Pod แล้วค่อยลด Pod เก่า]
อธิบายไฟล์ nginx-deployment.yaml โดยละเอียด
[
type: RollingUpdate = อัปเดตทีละส่วน ไม่ลบเก่าหมดก่อน
maxUnavailable: 1  = ยอมให้ Down Pod ได้เพียง 1 Pod
maxSurge: 1 = สร้าง Pod เกินจำนวนชั่วคราวได้ 1 Pod (รวมเป็น 4 Pods)
]
มี Program ที่จัดการ minikube แบบ GUI บน Windows 11 ไหม
[minikube dashboard]
คำสั่ง kubectl rollout status deployment/web --timeout=120s ทำงานอย่างไร
[รอจนกว่าจะ Update สำเร็จ โดยมีเวลาจำกัด 120 วินาที]
คำสั่ง kubectl get deploy,pods -l app=web ทำงานอย่างไร
[แสดงข้อมูล Deployment และ Pod ที่มี Label app=web]
ไฟล์ web-service.yaml มีหลักการทำงานอย่างไร
[ปิดให้ผู้ใช้งานภายนอก Cluster เข้าถึง Pod ที่มี Label app=web]
คำสั่ง kubectl get endpoints web-service ทำงานอย่างไร
[ดูว่า Service web-service กำลังส่ง Traffic ไปยัง Pod ไหนบ้าง]
คำสั่ง minikube service web-service --url ทำงานอย่างไร
[สร้าง URL ที่สามารถเข้าถึง Service จากเครื่อง Host]
ทำไมต้องรันคำสั่ง minikube service web-service --url อีกครั้ง
[เพราะ minikube รันบน Docker อีกที ไม่ใช่ Kubernates จริง]
อธิบาย ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
[ถ้าใช้ Kubernates จริง Client จะเข้าจาก nodePort: 30080
targetPort: 80 คือ port ของ Service
Pod:80 คือ port ของ Pod/Container
]
ไฟล์ lab6-commands.ps1 ทำหน้าที่อะไร ทำงานอย่างไร
[
1. ตวรจสอบเครื่องมือ
2. ล้าง Resource เดิม
3. สร้าง Voting App (Image จาก Docker) สร้าง Pod 5 ตัว, Service 4 ตัว
4. รอ Pod พร้อมใช้งาน
5. ตรวจว่าสร้าง Service และ Endpoint ครบ
6. แสดง URL
]
ทำไมถึงทำ Service redis และ db เป็น ClusterIP
[เป็นระบบหลังบ้าน ไม่ควรเปิดให้ผู้ใช้ภายนอกเข้าถึงโดยตรง]
ทำไมถึงทำ Service vote และ result เป็น NodePort
[เป็นหน้าเว็บที่ผู้ใช้ต้องเปิดผ่าน Browser และ NodePort ก็ถือว่ามีคุณสมบัติมองเห็นภายในเหมือน ClusterIP]
ทำไมไม่ทำ Service ของ worker ด้วย
[เพราะไม่มี Pod อื่นวิ่งเข้าหา worker มีแต่ worker วิ่งเข้าหา Pod อื่นจึงไม่ต้องสร้าง Service]
worker มีหน้าที่อะไร ไม่มีจะเกิดอะไรขึ้น
[ย้ายคะแนนจาก Redis ไปบันทึกใน PostgreSQL]
แล้วทำไมไม่เข้า PostgreSQL โดยตรง
[
Vote ได้เร็วกว่า
PostgreSQL ไม่ต้องรับ Request ทุกครั้ง
หาก DB ช้าชั่วคราว คะแนนยังสามารถรอใน Redis ได้
]
ไฟล์ db-pod.yaml และ db-service.yaml ทำงานอย่างไร
[
สร้าง Pod db ด้วย postgres:15-alpine port 5432
สร้าง Service db เป็นแบบ ClusterIP ชี้ไปที่ Pod db แล้วออก port ภายใน 5432
]
ไฟล์ redis-pod.yaml และ redis-service.yaml ทำงานอย่างไร
[
สร้าง Pod redis ด้วย redis:alpine port 6379
สร้าง Service redis เป็นแบบ ClusterIP ชี้ไปที่ Pod redis แล้วออก port ภายใน 6379
]
ไฟล์ result-pod.yaml และ result-service.yaml ทำงานอย่างไร
[
สร้าง Pod result ด้วย dockersamples/examplevotingapp_result port 80
สร้าง Service result เป็นแบบ NodePort ชี้ไปที่ Pod result แล้วออก port ภายใน 80 และภายนอก 31001
]
ไฟล์ vote-pod.yaml และ vote-service.yaml ทำงานอย่างไร
[
สร้าง Pod vote ด้วย dockersamples/examplevotingapp_vote port 80
สร้าง Service vote เป็นแบบ NodePort ชี้ไปที่ Pod vote แล้วออก port ภายใน 80 และภายนอก 31000
]
ไฟล์ worker-pod.yaml ทำงานอย่างไร
[
สร้าง Pod worker ด้วย dockersamples/examplevotingapp_worker ไม่มี listen port เพราะไม่รอรับ Request จาก Pod อื่น
]
ไฟล์ lab7-commands.ps1 ทำหน้าที่อะไร ทำงานอย่างไร
[
1. ตรวจสอบเครื่องมือ
2. สร้าง Deployment และ Service
3. รอ Deployment พร้อมใช้งาน
4. ทดสอบ Self Healing ด้วยการลบ Vote Pod 1 Pod
5. Scale Vote จาก 2 เป็น 3 Pod
6. ตรวจสอบ Endpoint
]
เราจะ Auto scale ตามโหลดอย่างไร เพราะตัวอย่างมันเป็น manual คำสั่งหมดเลย
[ใช้ HorizontalPodAutoscaler (HPA)
HPA จะวนตรวจทุก ~15 วินาที แล้วคำนวณว่าต้องใช้กี่ Pod ตามสูตรคร่าว ๆ:
เช่น มี 2 Pod, CPU จริงเฉลี่ย 100%, เป้าหมาย 50% → ต้องการ 4 Pod
]
ไฟล์ vote-hpa.yaml ทำงานอย่างไร
[
สร้าง HPA ชื่อ vote
กำหนดเป้าหมายที่จะควบคุมที่ Deployment ชื่อ vote
กำหนดช่วงจำนวน Pod
รักษา 50% ของ CPU Request ต่อ Pod
]
จากไฟล์ vote-deployment.yaml ส่วนนี้ทำงานอย่างไร สัมพันธ์กัน vote-hpa.yaml อย่างไร
        resources:
          requests:
            cpu: 100m #0.1 core
            memory: 64Mi
          limits:
            cpu: 250m
            memory: 128Mi 
[แต่ละ Pod ใช้อย่างน้อย 0.1 core / 64MiB ถ้าจะวาง Pod ใหม่ใน Node (ไม่ใช่ค่าใช้จริง) และได้ไม่เกิน 0.25 core / 128MiB
HPA จะพยายามรักษาให้ได้ 0.05 core / Pod หากสูงกว่าจะเปิด Pod ใหม่ ถ้ายังเหลือ Resource
]
ถ้า Pod ถูกสร้างใหม่ แล้วข้อมูลหาย จะแก้ปัญหานี้อย่างไร ตาม Best Practice
[ใช้งาน PersistentVolumeClaim หรือ StatefulSet ส่วน Production จะใช้ cloud managed database]
ไฟล์ lab8-gke.ps1 มีหน้าอะไร ทำงานอย่างไร
[
1. เช็คเครื่องมือ gcloud และ kubecrtl
2. ตั้งค่า gcloud ตั้ง project และ compute/zone
3. สร้าง cluster
4. ดึง credentials
5. deploy app + รอ rollout
6. รอสร้าง external ip
]
gcloud มีหน้าที่อะไร ทำไมต้องติดตั้ง
[เครื่อง CPI สำหรับติดต่อ GCP]
get-credentials ทำไมถึงต้องมีขั้นตอนนี้ มีหน้าที่อะไร
[ทำให้ kubectl เข้าถึง cluster GKE ได้]
คำสั่ง gcloud container clusters create voting-gke --num-nodes=2 --machine-type=e2-medium --zone=asia-southeast1-b ทำงานอย่างไร
[
สร้าง cluster ใหม่ ชื่อ 'voteing-gke'
สร้าง worker node 2 เครื่อง
แต่ละ node ใช้ VM ขนาด e2-medium
สร้างในโซนสิงคโปร์
เริ่มคิดเงินตั้งแต่สร้าง cluster
]
ทุกครั้งที่ apply kube gke มันจะได้ url external ของ vote กับ result ใหม่ใช่ไหม
[ไม่เปลี่ยนถ้าใช้ cluster เดิม หรือ re apply โดยไม่ลบ Service]
ถ้าเปลี่ยนใหม่ แล้วโดยทั่วไปเค้า set domain ให้ชี้ ip ยังไง
[จอง Static IP บน GCP]
ไฟล์ cleanup-cloud.ps1 มีหน้าอะไร ทำงานอย่างไร
[
1. ลบแอป
2. ลบ cluster ตาม Provider
]
ทำไมเมื่อ รัน gke up ใหม่ (ไม่ได้ลบ cluster เดิมทิ้ง) ข้อมูลใน PostgreSQL ยังคงอยู่
[ข้อมูลถูก mount ไปที่ PVC เมื่อสร้าง Pod ใหม่แต่อ้างอิง PVC ชื่อเดิม จะได้ข้อมูลเดิม หายเมื่อลบ cluster หรือ PVC โดยตรง
ไฟล์ข้อมูลฐานเก็บที่ PersistentVolumeClaim (PVC)
]
Postgres อยู่ใน cluster (PVC) หมายความว่าอะไร PVC คืออะไร
[PersistentVolumeClaim ใบขอพื้นที่เก็บข้อมูลถาวรจาก Kubernetes
PVC postgres-pvc  ──►  GCE Persistent Disk
]
ทำไมแนะนำให้ใช้ managed cloud SQL
[
ข้อมูลไม่หาย
Google จัดการ Operation ให้
]
นำ basic_app ขึ้น gke แบบ 2 pod max 10 ทำไม บอกว่า  max 10 pods แต่สร้าง autoscale 3 nodes ให้
[
max 10 pods = จำนวนที่มากที่สุดของ Pod (HPA)
max 3 nodes = จำนวนที่มากที่สุดของ Node
]
deploy บน push main GitHub action
ทำ RollingUpdate
[OK]
อธิบายไฟล์  lab9-commands.ps1 ว่ามีหน้าที่อะไร ทำงานอย่างไร
[
1. เช็คเครื่องมือ minikube และ kubectrl
2. apply Namespace / ConfigMap / Secret
3. รอ Pod Ready

]
namespace-voting คืออะไร ประโยชน์มีอะไรบ้าง
[
สร้าง Namespace ชื่อว่า 'voting-dev' สำหรับแยก Voting App ออกจาก Namespace อื่นใน Cluster
เสมือนสร้างกล่องแยกสำหรับเก็บ Pod / Deployment / Service/ ConfigMap / Secret
สามารถควบคุมสิทธิ์ RBAC ต่อ Namespace ได้
]
อธิบายไฟล์ namespace-voting.yaml โดยละเอียด
[
kind: Namespace = สร้าง Object ชนิด Namespace
name: voting-dev = ชื่อ Namespace
env: dev = label สำหรับ filter
app.kubernetes.io/part-of: voting-app = label สำหรับ filter
]
vote-configmap คืออะไร มีหน้าที่อะไรบ้าง
[เก็บค่า Config ที่ไม่ลับ]
อธิบายไฟล์ vote-configmap.yaml โดยละเอียด
[
ประกาศ ConfigMap ชื่อ voting-config ใน namespace voting-dev
kind: ConfigMap = สร้าง Object ชนิด ConfigMap
name: ชื่อ ConfigMap ไว้ใช้อ้างอิงใน Deployment
namespace: voting-dev = ให้เฉพาะ Deployment ใน namespace voting-dev เห็นเท่านั้น
data: = บล็อกข้อมูลแบบ key - string value
]
การใช้งาน ConfigMap แตกต่างจาก .env อย่างไร
[แนวคิดเหมือนกันคือ แยก config ออกจาก Code
ใช้ .env บน Local และ ConfigMap / Secret บน K8s]
นิยมใช้คู่กันไหม ระหว่าง .env และ ConfigMap / Secret แล้วถ้ามีชื่อตัวแปรเหมือนกัน จะถูก Override ไหม
[ใช้ .env บน Local และ ConfigMap / Secret บน K8s
ซ้ำ โดยมีลำดังนี้ .env -> ConfigForm -> Deployment env:]
db-secret คืออะไร มีหน้าที่อะไรบ้าง
[ที่เก็บรหัส แล้วส่งให้ Deployment db ผ่าน secretKeyRef
งาน Production ใช้ Vault / Cloud Secret Manager / CI สร้าง Secret ตอน Deploy]
อธิบายไฟล์ db-secret.yaml โดยละเอียด
[
kind: Secret = สร้าง Object ชนิด Secret
name: db-secret = ชื่อ Secret
namespace: voting-dev = กำหนดการมองเห็นเฉพาะ namespace voting-dev
type: Opaque = ประเภท Secret ทั่วไป
stringData: = ใส่ข้อความธรรมดา -> K8s แปลงเป็น base64
สร้าง Pod แล้วค่าถูกฉีดเข้าไป
]
อธิบายไฟล์ vote-deployment-config.yaml โดยละเอียด
[
ประกาศ Deployment โดยดึงค่า Config มาจาก ConfigMap
kind: Deployment = สร้าง Object ชนิด Deployment
metadata.name: vote = ชื่อ Deployment
metadata.namespace: voting-dev = ระบุ Namespace
replicas: 1 = จำนวน Pod ที่ต้องการ
selector.matchLabels.app: vote = ถือครอง Pod ชื่อ  'vote'
template = แม่แบบการสร้าง Pod
name: vote = ชื่อ Container ใน Pod
image: dockersamples/examplevotingapp_vote = Image ที่ใช้
ports.containerPort: 80 = ฟังที่ Port 80 ของ Container
envFrom.configMapRef.name: vote-config = นำเข้าค่าจาก ConfigMap
]
อธิบายไฟล์ db-deployment-secret.yaml โดยละเอียด
[
ประกาศ Deployment ของ Postgres ใน namespace 'voting-dev' และดึง Secret มาจาก 'db-secret'
kind: Deployment = สร้าง Object Deployment
metadata.name: db = ชื่อ Deployment
metadata.namespace: voting-dev = ระบุ Namespace
metadata.labels.app: db = Label ของ Deployment
replicas: 1 = สร้าง 1 Pod
selector.matchLabels.app: db = Deployment ถือครอง Pod ที่ app:db
template.metadata.labels.app: db = Label บน Pod
name: postgres = ชื่อ Container
image: postgres:15-alpine = Image ที่ใช้
containerPort: 5432 = เปิด Port 5432 ที่ Container
env.name.valueFlorm.secretKeyRef.name และ key = นำเข้าค่าจาก Secret 'db-secret'
ถ้าชื่อ Key บน Image กับ Secret ไม่ตรงกัน ต้อง map ที่ละตัวแปร
]
Deployment vote เชื่อมต่อกับ postgres อย่างไร กำหนดที่ไหน
[จากตัวอย่างเก่า vote ไม่ได้เชื่อมกับ postgres โดยตรง แต่เชื่อมกับ redis
image ของ vote มองหา service ชื่อ redis]
Pod ใน voting-dev เรียก Service db ใน default ได้ไหม — ต้องใช้ DNS แบบไหน?
[ได้ <service>.<namespace>.svc.cluster.local เช่น db.default.svc.cluster.local หรือ 	
db.default]
Label env: dev บน Namespace มีผลต่อการรันแอปไหม หรือมีไว้ทำอย่างอื่น?
[ไม่มีผล ใช้ filter เท่านั้น ถ้าลบออก แอปยังรันเหมือนเดิม]
แยก voting-dev กับ voting-staging ใน cluster เดียว แก้ปัญหาอะไรได้บ้าง และยังแก้ไม่ได้อะไร?
[
แก้ได้
ชื่อ Resource ชนกัน เพราะแยก namespace
แยก Config คนละชุดได้
RBAC แยกต่อ namespace ได้
จำลองหลาย env บน cluster เดียวกันได้
แก้ไม่ได้
ไม่เท่ากับแยก Infra, แยกเครือข่าย, แยกสิทธิ์ และความทนทาน
มักต้องเติม NetworkPolicy, RBAC, ResourceQuota หรือ Cluster แยก
]
envFrom กับ mount ConfigMap เป็นไฟล์ เหมาะกับสถานการณ์ต่างกันอย่างไร?
[
envForm อ่านทั้งไฟล์ ไฟล์ไม่ใหญ่มาก / printenv แล้วค่าแสดงทั้งหมด ต้อง reload pod ใหม่ เมื่อค่าเปลี่ยน
volumeMounts แต่ละ key กลายเป็นไฟล์แยกใต้ /etc/config/ แล้วอ่านไฟล์เอา เหมาะเมื่อไฟล์ใหญ่ ไม่ต้อง reload
]
แก้ ConfigMap แล้ว Pod ที่รันอยู่เห็นค่าใหม่ทันทีไหม? ต้องทำอะไรเพิ่ม?
[หากใช้ envForm ต้อง rollout restart หรือสร้าง Pod ใหม่
หากใช้ volumeMounts ให้ทำ hot reload การอ่านไฟล์]
base64 ใน Secret แปลว่า “เข้ารหัสปลอดภัย” หรือแค่ encoding?
[encoding ใครมีสิทธิ์ดู secret จะถอดรหัสได้ (มี RBAC เพิ่มเติม)]
ทำไม lab ใช้ stringData แต่ kubectl get secret -o yaml มักเห็นเป็น data?
[stringData ให้พิมพ์รหัสแบบ plain อ่านง่าย - cluster เก็บและโชว์กลับเป็น data encoding เสมือ]
type: Opaque หมายความว่าอะไร ต่างจาก Secret แบบ TLS อย่างไร?
[Opaque = Secret ทั่วไป
TLS = tls.crt และ tls.key]
ทำไม db-secret.yaml เตือนว่าอย่า commit รหัส production — แล้วงานจริงควรเก็บรหัสที่ไหน?
[git จำประวัติ เก็บที่ Cloud Secret Manager / Vault  / CICD Secret]
ใครก็ตามที่ get secret ใน namespace ได้ จะอ่าน password ได้ง่ายแค่ไหน?
[ง่ายมาก decoding ได้]
ทำไม vote ใช้ envFrom.configMapRef แต่ db ใช้ secretKeyRef ทีละตัว?
[envFrom.configMapRef = แอปต้อง env ชื่อเดียวกัน
secretKeyRef = image ต้องการชื่อตายตัว เลยต้อง map ทีละตัว]
ถ้า key ใน Secret คือ username แต่ Postgres ต้องการ POSTGRES_USER — จะแก้ที่ Secret หรือที่ Deployment ดีกว่า?
[แก้ Deployment เมื่อ secret ใช้ร่วมกันหลาย app
แก้ Secret เมื่อทุก app ใช้เป็นเรื่องเดียวกัน]
POSTGRES_DB: postgres ใส่ตรงๆ ใน Deployment ได้เพราะอะไร — ควรย้ายไป ConfigMap ไหม?
[ไม่ใช่ความลับ ย้ายไป ConfigMap ได้ แต่ไม่บังคับ]
ถ้า ConfigMap กับ Secret มี key ชื่อซ้ำ และฉีดเข้า container เดียวกัน ค่าไหนชนะ?
[จะใช้ค่าที่ประกาศหลังสุด]
เทียบแนวคิด (.env / image / hard-code)
[
1. Hard-code ค่าเปลี่ยนต้องแก้ code -> Build ใหม่ / รหัสหลุดใน git
2. ENV ใน Dockerfile ค่าเปลี่ยนต้อง build/push image ใหม่
3. .env (local) ใช้เมื่อ local
]
ถ้าใส่ password ใน Dockerfile เป็น ENV แล้วมาทับด้วย Secret ใน K8s — อันไหนมีผลตอนรัน?
[Secret]
ทำไม Lab 9 deploy แค่ vote + db — พอโหวตแล้วข้อมูลลง Postgres ได้เลยไหม?
[ไม่ได้ เพราะ Service ไม่ครบ]
vote เชื่อม Postgres ตรงๆ หรือไม่ — ถ้าไม่ ใครเป็นตัวเชื่อม และกำหนดที่ไหน?
[ไม่เชื่อมตรง ๆ worker อ่านโหวตจาก Redis แล้วเขียนลง Postgres
กำหนดที่ Image ของ vote ว่าให้คุยกับ Redis]
lab9-commands.ps1 apply Namespace/Config/Secret ก่อน Deployment เพราะเหตุใด?
[เพราะ Deployment พึ่งพา Object เหล่านั้นตอนสร้าง Pod]
คำสั่ง printenv | Select-String "OPTION_" พิสูจน์อะไรได้ และพิสูจน์ไม่ได้อะไร?
[พิสูจน์ว่า ConfigMap ถูกฉีดเป็น env ใน Pod vote สำเร็จ แต่ไม่ได้บอกว่าแอปทำงานถูกต้อง]
ตรวจว่า db ได้ password จาก Secret จริง ควรใช้วิธีไหนที่ปลอดภัยกว่าการ printenv โชว์รหัส?
[kubectl get secret db-secret -n voting-dev
ตรวจว่า Secret มี key ที่ต้องการ โดยไม่ decode
หรือ
kubectl describe pod -n voting-dev -l app=db
ดูว่าไม่มี CreateContainerConfigError และมีการอ้างอิง db-secret / key password
]
เมื่อไหร่ควรใช้ External Secrets / Vault แทน Secret ธรรมดาใน YAML?
[เมื่อเป็นรหัสที่สำคัญต่อธุรกิจ]
ทำไมการไม่ Hard-code ค่าตัวแปร (เช่น Feature Flags หรือค่าเริ่มต้น) ไว้ใน Container Image หรือไฟล์ Deployment จึงสำคัญที่สุดเมื่อต้องนำแอปพลิเคชันไปรันในหลาย Environment (Dev, Staging, Prod)?
[
1. Build Image ครั้งเดียว นำไปรันได้ทุก environment
2. ควบคุมความปลอดภัย ไม่ให้ข้อมูลความลับรั่วไหล
3. Image เดียวกัน รันทุกสภาพแวดล้อม มั่นใจว่ารันได้เหมือนกันทุก environment
4. ปรับแต่งค่าได้ทันที ไม่ต้อง Rebuild
]
ไฟล์ lab10-commands.ps1 ทำหน้าที่อะไร ทำงานอย่างไร
[ทดสอบการ Deploy พร้อม Resource Limit และ Liveness Probes]
Probes คืออะไร
[
Probes = Kubernates ตรวจสอบ Container เป็นระยะว่าพร้อมรับ traffic หรือไม่
1. Readiness = ล้มแล้ว ไม่ส่ง traffic ไป Pod นั้น / ใช้ตอนแอปกำลัง Boot
2. Liveness = restart container เมื่อไม่ผ่านหลายครั้ง
3. Startup = หน่วงเวลา Readiness/Liveness ช่วงแอปกำลัง Boot นาน
]
วิธีเช็ค Probes ทำอย่างไรได้บ้าง
[
1. httpGet = ส่ง GET ไปเป็นระยะ
2. tcpSocket = เช็คว่าพอร์ตเปิดอยู่
3. exec = รันคำสั่งใน Container
]
อธิบายไฟล์ web-deployment-probes.yaml โดยละเอียด
[
readinessProbe: 
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 5
ตรวจ Readiness ด้วยวิธี httpGet ที่ port 80 หลัง Container start 3 วินาที เช็คทุก 5 วินาที

livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
ตรวจ Liveness ด้วยวิธี httpGet ที่ port 80 หลัง Container start 10 วินาที เช็คทุก 10 วินาที
]
เช็ค Readiness ทุก 5 วิตลอดไปเลยใช่หรือไม่
[ใช่ ตลอดอายุของ Container ถ้าไม่ผ่าน ตัดออกจาก Endpoint ไม่ restart]
เช็ค Liveness ทุก 10 วิตลอดไปเลยใช่หรือไม่
[ใช่ ตลอดอายุ Container]
ถ้ามันเช็คตลอดอายุของ Container แล้วอะไรคือความแตกต่างระหว่าง Readiness และ Liveness
[
1. ในงานจริง มักยิง url คนละเส้น
2. ถ้าไม่ผ่าน Readiness จะไม่นำ Pod นั้นให้ผู้ใช้ ไม่ restart / Liveness จะ restart ให้]
นิยมใช้คู่กันไหม หรือสามารถใช้อย่างใดอย่างหนึ่งก็เพียงพอแล้ว
[มักใช้คู่กัน คือ อย่าส่ง request ตอนยังไม่พร้อม และ restart ถ้า Container ค้าง]
ไฟล์ lab11-commands.ps1 ทำหน้าที่อะไร ทำงานอย่างไร
[
1. เปิด metrics-server รอจน kubectl top ผ่าน
2. ใส่ CPU request 50% แล้ว apply HPA ใน namespace 'voting-dev'
3. แสดง log
]
อธิบายไฟล์ vote-hpa.yaml โดยละเอียด
[
kind: HorizontalPodAutoscaler = สร้าง Object HorizontalPodAutoscaler ตัวเพิ่มลด Pod
name: vote = ชื่อ HPA
namespace: voting-dev = ระบุ namespace
scaleTargetRef.name: vote = ชี้ไปที่ deployment 'vote'
minReplicas: 2 = อย่างน้อย 2 Pod
maxReplicas: 6 = เพิ่มได้สูงสุด 6 Pod
metrics.type: Resource = ใช้ resource ของ Pod เป็นตัวกำหนด
metrics.name: cpu = ดู cpu
metrics.type: Utilization = ดูที่ % ของ request
metrics.averageUtilization: 50 = เฉลี่ย 50% ของ Pod
]
"รักษา CPU เฉลี่ยให้ใกล้ 50% ของ request" มีความหมายว่าอย่างไร ของ Scenario ประกอบ 
[HPA รักษาให้ทุก Pod ใช้ CPU จริง เท่ากับครึ่งหนึ่ง (50%) ของ CPU ที่จองไว้]
เกิดอะไรขึ้นเมื่อ OOMKill 
[Pod ถูก kill เพราะใช้ RAM เกิน จากนั้น K8s สร้าง Pod ใหม่]
ถ้าตั้งแค่ limits ไม่มี requests scheduler กับ HPA จะได้รับผลกระทบอย่างไร?
[Scheduler อาจนำ Pod ไปวางบน Node ที่มี Resource ที่ไม่เพียงพอ ทำให้ระบบไม่เสถียร
HPA ไม่สามารถ Autoscale ให้ได้
]
ทำไม CPU เกิน limit ถึง throttle แต่ memory เกินถึง OOMKill?
[
CPU สามารถบีบอัด โดยลดความเร็วการประมวลผลได้ จึง throttling ช้าแต่ใช้งานต่อได้
Memory บีบอัดไม่ได้ จึงต้อง Kill Pod เพื่อป้องกันไม่ให้ Node ค้าง
]
ตั้ง requests สูงเกินความจริง มีข้อเสียอะไรต่อความหนาแน่นของ cluster?
[
Cluster เต็มเร็ว แม้ทรัพยากรยังเหลือ เพราะถูก Reserve ไว้ให้ Pod
บน Cloud เสียค่าใช้จ่ายมากขึ้น เพราะต้อง Scale Node มากขึ้น
]
จะรู้ได้อย่างไรว่าควรตั้ง Request และ Limit ที่เท่าไหร่จึงจะเพียงต่อ App ของเรา
[
1. ดูจาก History Metric ช่วง Staging
2. Load Testing ด้วย JMeter
3. ใช้ Program VPA Recommender
]
อธิบายว่าไฟล์ day-08/lab12-commands.ps1 มีหน้าที่อะไร ทำงานอย่างไร
[
1. เช็คว่ามี namespace voting-dev แล้วหรือยัง
2. สร้าง PVC ชื่อ postgres-data
3. deploy db และ mount PVC ชื่อ 'postgres-data' ด้วยคำสั่ง volumeMounts
4. เช็คว่า Mount สำเร็จ
]
อธิบายส่วนที่สำคัญของ postgres-pvc.yaml
[
kind: PersistentVolumeClaim = ขอสร้าง PersistentVolume
name: postgres-data = ชื่อ PVC
namespace: voting-dev = ชื่อ namespace
ReadWriteOnce = เขียว อ่าวพร้อมกันได้แค่ node เดียวเท่านั้น ป้องกัน Data Corruption
storage: 1Gi = จองพื้นที่ 1GB
]
อธิบายว่าไฟล์ day-09/lab13-commands.ps1 มีหน้าที่อะไร ทำงานอย่างไร
[
1. เปิดใช้งาน ingress
2. deploy app 'result'
3. สร้าง ingress
]
อธิบายส่วนสำคัญของ voting-ingress.yaml
[
voting.local ตั้งให้ วิ่งไปที่ service 'vote'
voting.local/result ตั้งให้ วิ่งไปที่ service 'result'
]
ในเมื่อจะใช้ cloud kubernates อยู่แล้ว ทำไมถึงต้องเรียน ingress
[เพราะใช้ Concept เดียวกัน]
Ingress คืออะไร ช่วยเล่า Scenario ที่มี และไม่มีมันหน่อย
[ทางเข้า HTTP/HTTPS จากข้างนอก Cluster เข้ามาหา Services ภายใน
ถ้าไม่มี ต้องเปิด port แต่ละ Services
ถ้ามี สามารถตั้ง routing ได้
จัดการ HTTPS ที่จุดเดียว
มี Concept คล้าย Nginx
ทำหน้าที่เป็น Reverse Proxy / Router สำหรับ Kubernetes
]
เล่า Use Case ยอดนิยมของ Ingress บน Cloud
[
ให้เข้าถึงด้วย host url หรือ subdomain แทนที่จะเปิด port มีประโยชน์มากใน Microservices
จัดการ https
แบ่ง env เช่น dev staging และ prod
จัดการ Rolling Update
จุดรวม Security เช่น Rate Limit / Authentication
Load Balancer แบบ 1 ตัว ลดค่าใช้จ่าย แทนที่จะมี 1 Load Balancer ต่อ 1 Service
]


รวมคำสั่ง
minikube start --driver=docker
kubectl delete -f web-deployment.yaml
kubectl delete -f web-deployment.yaml --ignore-not-found
kubectl apply -f web-deployment.yaml
kubectl apply -f web-service.yaml
kubectl get svc web-service
minikube service web-service --url
minikube addons enable metrics-server
kubectl get hpa vote --watch
kubectl get pods -l app=vote --watch
gcloud container clusters create voting-gke --num-nodes=2 --machine-type=e2-medium --zone=asia-southeast1-b